services:
  - type: web
    name: petlove-backend
    env: python
    buildCommand: cd backend_python && pip install -r requirements.txt
    startCommand: cd backend_python && uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0